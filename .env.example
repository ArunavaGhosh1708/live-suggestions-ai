# Client-side only — paste this in client/.env.local
VITE_API_URL=http://localhost:8000

# No server-side env vars required.
# The Groq API key is entered in the Settings modal and stored in localStorage.
